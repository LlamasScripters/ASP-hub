{
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"husky": "9.1.7",
		"lint-staged": "16.0.0"
	},
	"scripts": {
		"dev": "docker compose -f compose.dev.yml up --build --watch",
		"dev:build": "docker compose -f compose.dev.yml up --build --force-recreate",
		"dev:logs": "docker compose -f compose.dev.yml logs -f",
		"dev:down": "docker compose -f compose.dev.yml down",
		"db:migrate": "docker compose -f compose.dev.yml exec server npm run db:migrate",
		"db:seed": "docker compose -f compose.dev.yml exec server npm run db:seed",
		"db:reset": "docker compose -f compose.dev.yml exec server npm run db:reset",
		"db:fresh": "docker compose -f compose.dev.yml exec server npm run db:fresh",
		"db:studio": "cd server && npm run db:studio",
		"prepare": "husky",
		"lint": "biome check .",
		"lint:fix": "biome check --write ."
	},
	"dependencies": {}
}
